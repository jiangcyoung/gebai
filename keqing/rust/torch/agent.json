{
  "name": "torch",
  "description": "",
  "protocol": "2",
  "command": ["{lang_dir}/target/release/torch{exe}"],
  "build": {
    "command": ["cargo", "build", "--release", "-p", "torch", "--manifest-path", "{lang_dir}/Cargo.toml"]
  },
  "requiresApproval": false
}
